<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--    <TextView
            android:id="@+id/tv_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textColor="@color/black"
            android:paddingHorizontal="@dimen/margin_small"
            android:paddingVertical="@dimen/margin_x_tiny"
            android:textAppearance="@style/Theme.MovieBD.Genres"
            tools:text="HORROR" />-->

    <com.boyzdroizy.library.tooltipopwordtv.wordTextView.SelectableWordTextView
        android:id="@+id/tv_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="@style/Theme.MovieBD.Body"
        android:lineSpacingExtra="7sp"
        app:highlightBackgroundColor="@color/light_purple"
        app:highlightTextColor="@color/white"
        app:setUnderline="true"
        android:paddingHorizontal="@dimen/margin_xx_tiny"
        android:paddingVertical="@dimen/margin_x_tiny"
        tools:text="HORROR"/>

</RelativeLayout>